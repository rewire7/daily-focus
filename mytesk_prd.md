# 📋 할 일 관리 앱 — 제품 요구사항 정의서 (PRD)

| 항목 | 내용 |
|---|---|
| **문서 버전** | v1.0 |
| **작성일** | 2026-07-26 |
| **작성자** | 이카루스 |
| **제품명(가칭)** | Daily Focus |
| **상태** | 초안 (Draft) |

---

## 1. 개요

### 1.1 목적
하루 **10~20개** 수준의 개인 할 일을 브라우저에서 즉시 기록하고, 카테고리별로 분류하며, 완료 진행률을 한눈에 확인할 수 있는 **단일 사용자용 웹 앱**을 만든다.

### 1.2 배경
기존 상용 할 일 앱(Todoist, Notion 등)은 개인이 하루치 할 일을 관리하기엔 기능이 과하고, 로그인·동기화·구독 같은 진입 장벽이 있다. 이 프로젝트는 **"열면 바로 쓰는"** 최소한의 도구를 목표로 한다.

### 1.3 타겟 사용자
- 1인(본인) 전용
- 데스크톱 브라우저 주 사용, 모바일 브라우저에서도 읽고 체크 가능한 수준

> 💡 **핵심 설계 철학**: 기능을 늘리는 것보다 **입력 마찰(friction)을 0에 가깝게** 만드는 게 우선이다. 할 일 앱이 실패하는 가장 큰 이유는 기능 부족이 아니라 "쓰기 귀찮아서"다.

---

## 2. 목표 및 성공 지표

| 목표 | 성공 지표 |
|---|---|
| 빠른 입력 | 페이지 로드 후 **3초 이내**에 할 일 1건 추가 가능 (클릭 없이 키보드만으로) |
| 데이터 보존 | 새로고침·브라우저 재시작 후 데이터 **100% 유지** |
| 진행 현황 파악 | 화면 상단에서 **스크롤 없이** 전체/카테고리별 진행률 확인 |
| 성능 | 항목 100개 기준 렌더링 **100ms 이내** |
| 단순성 | 외부 라이브러리 **0개**, 전체 코드 **500줄 내외** |

---

## 3. 범위 정의

### ✅ 포함 (In Scope)
- 할 일 추가 / 수정 / 삭제 (CRUD[^1])
- 완료 체크 및 해제
- 카테고리 분류: 업무 / 개인 / 공부
- 전체 및 카테고리별 진행률 표시
- 카테고리·상태별 필터링
- `localStorage`[^2] 기반 데이터 영속성[^3]

### ❌ 제외 (Out of Scope) — v1 기준
| 제외 기능 | 사유 |
|---|---|
| 로그인 / 계정 | 1인 사용, 서버 불필요 |
| 클라우드 동기화 | 백엔드 구축 비용 대비 효용 낮음 |
| 마감일 / 알림 | v2 후보. v1은 "오늘 할 일"에 집중 |
| 반복 일정 | 상태 관리 복잡도가 급증 |
| 하위 작업(subtask) | 10~20개 규모엔 과설계 |
| 협업 / 공유 | 개인용 목적과 불일치 |
| 다크 모드 | 있으면 좋지만 v1 필수 아님 |

---

## 4. 사용자 스토리

| ID | 스토리 |
|---|---|
| US-1 | 사용자로서, 아침에 앱을 열고 **키보드만으로** 오늘 할 일 15개를 빠르게 입력하고 싶다. |
| US-2 | 사용자로서, 할 일을 **업무/개인/공부**로 나눠 지금 집중할 영역만 보고 싶다. |
| US-3 | 사용자로서, 작업을 끝낼 때마다 **체크**하고 진행률이 올라가는 걸 보며 동기부여를 받고 싶다. |
| US-4 | 사용자로서, 오타나 내용 변경 시 **항목을 직접 수정**하고 싶다. |
| US-5 | 사용자로서, 실수로 새로고침해도 **데이터가 그대로** 있길 원한다. |
| US-6 | 사용자로서, 완료된 항목은 **숨겨서** 남은 일에만 집중하고 싶다. |

---

## 5. 기능 요구사항

### F-01. 할 일 추가

**설명** — 입력 필드에 내용을 입력하고 카테고리를 선택해 목록에 추가한다.

**상세 요구사항**
- 입력 필드 + 카테고리 선택(드롭다운 또는 버튼) + 추가 버튼 구성
- `Enter` 키로 추가 가능 (필수)
- 추가 후 입력 필드는 비워지고 **포커스 유지** → 연속 입력 가능
- 카테고리 선택값은 추가 후에도 **유지** (같은 카테고리 연속 입력 대응)
- 신규 항목은 목록 **최상단**에 추가

**수용 기준 (Acceptance Criteria)[^4]**
- [ ] 빈 문자열 또는 공백만 입력 시 추가되지 않고, 시각적 피드백(입력창 흔들림 등) 제공
- [ ] 최대 200자 제한, 초과 시 입력 차단
- [ ] 추가 즉시 `localStorage`에 저장됨
- [ ] 카테고리 미선택 시 기본값 `업무`로 지정

---

### F-02. 할 일 목록 조회

**설명** — 저장된 할 일을 목록 형태로 표시한다.

**상세 요구사항**
- 각 항목 표시 요소: 완료 체크박스 · 내용 · 카테고리 배지 · 수정 버튼 · 삭제 버튼
- 정렬 순서: **미완료 항목 우선 → 최신 생성순**
- 완료 항목은 취소선 + 흐린 색상 처리
- 카테고리별 색상 구분 (예: 업무 🔵 / 개인 🟢 / 공부 🟠)
- 목록이 비었을 때 빈 상태(Empty State) 메시지 표시

**수용 기준**
- [ ] 항목 100개에서도 스크롤 렌더링이 끊기지 않음
- [ ] 완료 항목 체크 시 즉시 위치가 재정렬됨

---

### F-03. 할 일 수정

**설명** — 기존 항목의 내용과 카테고리를 변경한다.

**상세 요구사항**
- 항목 텍스트 **더블클릭** 또는 수정 버튼 클릭 시 인라인 편집 모드 진입
- 편집 모드에서 텍스트 전체 선택 상태로 포커스
- `Enter` → 저장 / `Esc` → 취소
- 입력 영역 밖 클릭(blur) 시 자동 저장
- 카테고리 배지 클릭 시 카테고리 순환 변경

**수용 기준**
- [ ] 빈 값으로 저장 시도 시 원래 값으로 복구
- [ ] 동시에 두 항목이 편집 모드에 들어가지 않음

---

### F-04. 할 일 삭제

**설명** — 항목을 목록에서 영구 제거한다.

**상세 요구사항**
- 각 항목에 삭제 버튼(🗑) 배치
- 삭제 시 확인 절차 필요 — **권장 방식**: 확인 창(confirm) 대신 **"실행 취소(Undo)" 토스트**를 3초간 노출
- 부가 기능: "완료된 항목 전체 삭제" 버튼

**수용 기준**
- [ ] 삭제 후 진행률이 즉시 재계산됨
- [ ] Undo 클릭 시 원래 위치·상태 그대로 복원

> ⚠️ **설계 노트**: `confirm()` 팝업은 매번 클릭을 강요해 마찰을 키운다. 하루 20개를 다루는 앱에서는 **낙관적 삭제[^5] + Undo** 패턴이 체감 속도 면에서 훨씬 낫다.

---

### F-05. 완료 체크

**설명** — 항목의 완료 여부를 토글한다.

**상세 요구사항**
- 체크박스 클릭 시 `completed` 상태 반전
- 완료 시각(`completedAt`) 기록
- 완료 시 짧은 시각적 피드백 (체크 애니메이션 등)
- 완료 → 미완료 되돌리기 가능

**수용 기준**
- [ ] 토글 즉시 진행률과 정렬 순서 반영
- [ ] `localStorage` 즉시 반영

---

### F-06. 카테고리 분류 및 필터

**설명** — 할 일을 업무/개인/공부로 나누고 필터링한다.

**상세 요구사항**

| 카테고리 | 내부 값 | 색상(제안) |
|---|---|---|
| 업무 | `work` | 파랑 `#3B82F6` |
| 개인 | `personal` | 초록 `#10B981` |
| 공부 | `study` | 주황 `#F59E0B` |

- 상단 필터 탭: `전체` / `업무` / `개인` / `공부`
- 상태 필터: `전체` / `미완료` / `완료` (카테고리 필터와 **독립 동작**)
- 각 필터 탭에 해당 개수 표시 (예: `업무 (7)`)
- 선택한 필터는 `localStorage`에 저장 → 새로고침 후에도 유지

**수용 기준**
- [ ] 두 필터 조합 시 교집합으로 정확히 필터링
- [ ] 필터 결과가 0건일 때 안내 메시지 표시

---

### F-07. 진행률 표시

**설명** — 완료 비율을 시각적으로 보여준다.

**상세 요구사항**
- 화면 상단 고정 영역에 배치
- 전체 진행률: 프로그레스 바 + `완료 8 / 전체 15 (53%)` 형식 텍스트
- 카테고리별 진행률: 미니 바 3개 또는 텍스트 요약
- 계산식:

$$\text{진행률(\%)} = \left\lfloor \frac{\text{완료 항목 수}}{\text{전체 항목 수}} \times 100 \right\rfloor$$

- 전체 항목 0개일 때 `0%`로 표시 (0으로 나누기 방지)
- 100% 달성 시 축하 메시지 또는 시각 효과

**수용 기준**
- [ ] 진행률은 **필터와 무관하게 전체 데이터 기준**으로 계산
- [ ] 항목 추가/삭제/체크 시 즉시 갱신

---

### F-08. 데이터 영속성

**설명** — 브라우저를 닫았다 열어도 데이터가 유지된다.

**상세 요구사항**
- 저장소: `localStorage`
- 저장 키: `dailyfocus.todos.v1`, `dailyfocus.filters.v1`
- 저장 시점: 모든 상태 변경 직후 (추가/수정/삭제/체크/필터 변경)
- 앱 로드 시 저장된 데이터 복원
- 손상된 JSON일 경우 예외 처리 후 빈 배열로 초기화 (앱이 죽지 않을 것)
- 스키마 버전 필드 포함 → 향후 마이그레이션 대비

**수용 기준**
- [ ] 새로고침 후 모든 데이터·필터 상태 동일하게 복원
- [ ] `localStorage` 접근 불가 환경(사생활 보호 모드 등)에서 앱이 크래시하지 않고 경고 메시지 표시

> ⚠️ **알아둘 제약**: `localStorage`는 **브라우저·기기별로 격리**된다. 집 PC와 회사 PC의 데이터는 공유되지 않고, 브라우저 데이터 삭제 시 함께 지워진다. v1은 이 트레이드오프를 수용하되, **JSON 내보내기/가져오기 버튼**을 넣어두면 백업 수단이 생긴다. (아래 F-09 참고)

---

### F-09. 데이터 백업 (선택 구현, 권장)

**설명** — 데이터를 JSON 파일로 내보내고 다시 불러온다.

**상세 요구사항**
- `내보내기` 버튼 → `todos-YYYYMMDD.json` 다운로드
- `가져오기` 버튼 → 파일 선택 후 데이터 복원 (덮어쓰기 여부 확인)

> 구현 비용은 20줄 남짓인데, `localStorage` 유실 리스크를 통째로 상쇄한다. 가성비가 가장 좋은 기능이다.

---

## 6. 데이터 모델

### 6.1 할 일 객체

```javascript
{
  id: "1721980800000-a3f9",   // 생성시각 + 랜덤 문자열
  title: "PRD 초안 작성하기",     // string, 1~200자
  category: "work",            // "work" | "personal" | "study"
  completed: false,            // boolean
  createdAt: 1721980800000,    // Unix timestamp (ms)
  completedAt: null            // number | null
}
```

### 6.2 localStorage 저장 구조

```javascript
// key: "dailyfocus.todos.v1"
{
  version: 1,
  todos: [ /* 할 일 객체 배열 */ ]
}

// key: "dailyfocus.filters.v1"
{
  category: "all",   // "all" | "work" | "personal" | "study"
  status: "all"      // "all" | "active" | "completed"
}
```

---

## 7. 화면 구조

```
┌──────────────────────────────────────────────┐
│  📋 Daily Focus            [내보내기] [가져오기] │
├──────────────────────────────────────────────┤
│  전체 진행률                                    │
│  ████████████░░░░░░░░░  8 / 15 (53%)          │
│  업무 4/7 · 개인 3/5 · 공부 1/3                 │
├──────────────────────────────────────────────┤
│  [ 할 일을 입력하세요...        ] [업무▾] [추가] │
├──────────────────────────────────────────────┤
│  [전체 15] [업무 7] [개인 5] [공부 3]           │
│  [전체] [미완료] [완료]                         │
├──────────────────────────────────────────────┤
│  ☐  주간 보고서 초안 작성        🔵업무  ✏️ 🗑   │
│  ☐  운동 30분                  🟢개인  ✏️ 🗑   │
│  ☑  ~~영어 단어 50개~~          🟠공부  ✏️ 🗑   │
├──────────────────────────────────────────────┤
│              [완료된 항목 삭제]                  │
└──────────────────────────────────────────────┘
```

---

## 8. 기술 사양

### 8.1 기술 스택
| 영역 | 선택 |
|---|---|
| 마크업 | HTML5 |
| 스타일 | CSS3 (Flexbox / CSS 변수) |
| 로직 | Vanilla JavaScript (ES6+) |
| 저장소 | Web Storage API — `localStorage` |
| 빌드 도구 | **없음** — 파일 열면 바로 실행 |
| 외부 의존성 | **없음** |

### 8.2 파일 구조
```
daily-focus/
├── index.html
├── style.css
└── app.js
```
> 단일 파일(`index.html`)에 전부 넣어도 무방하지만, 3개로 분리하는 편이 수정·확장에 유리하다.

### 8.3 아키텍처 패턴

**단방향 데이터 흐름**을 권장한다.

```
사용자 이벤트
   ↓
state 변경 (todos 배열 조작)
   ↓
save()   — localStorage 반영
   ↓
render() — 화면 전체 재그리기
```

- 전역 상태 하나(`let state = { todos: [], filters: {} }`)만 유지
- DOM을 부분 수정하지 않고 목록 영역을 **통째로 다시 그린다** → 상태 불일치 버그 원천 차단
- 항목 100개 수준에서는 성능 문제 없음 (전체 재렌더링 비용 < 10ms)
- 이벤트는 목록 컨테이너에 **이벤트 위임**[^6]으로 하나만 등록

### 8.4 핵심 함수 시그니처(제안)

```javascript
loadState()                     // localStorage → state
saveState()                     // state → localStorage
addTodo(title, category)
updateTodo(id, changes)
deleteTodo(id)
toggleTodo(id)
getFilteredTodos()              // 필터 적용된 배열 반환
calcProgress(todos)             // { total, done, percent }
render()                        // 전체 UI 갱신
```

---

## 9. 비기능 요구사항

| 항목 | 요구사항 |
|---|---|
| **성능** | 항목 100개 기준 렌더링 100ms 이내 |
| **브라우저 호환** | Chrome / Edge / Firefox / Safari 최신 2개 버전 |
| **반응형** | 320px 이상 화면에서 레이아웃 깨지지 않을 것 |
| **접근성** | 체크박스·버튼에 `aria-label` 부여, `Tab` 키만으로 전체 조작 가능 |
| **보안** | 사용자 입력을 `innerHTML`이 아닌 **`textContent`로 삽입** — XSS[^7] 차단 |
| **안정성** | `localStorage` 파싱 실패·용량 초과(`QuotaExceededError`) 시 앱이 죽지 않고 안내 메시지 표시 |

---

## 10. 엣지 케이스 체크리스트

- [ ] 빈 값 / 공백만 입력
- [ ] 200자 초과 입력
- [ ] 이모지·특수문자·HTML 태그(`<script>`) 입력
- [ ] 항목 0개 상태 (진행률 0으로 나누기)
- [ ] 전체 항목 완료 (100%)
- [ ] 필터 결과 0건
- [ ] `localStorage` 데이터가 수동으로 손상된 경우
- [ ] 시크릿 모드 / `localStorage` 차단 환경
- [ ] 같은 앱을 두 탭에서 동시에 연 경우 → **v1은 마지막 저장 우선(last-write-wins)** 으로 수용

---

## 11. 개발 마일스톤

| 단계 | 범위 | 산출물 | 예상 |
|---|---|---|---|
| **M1** | 뼈대 + 추가/조회 + localStorage | 입력하고 새로고침해도 남아있음 | 1일 |
| **M2** | 완료 체크 + 삭제 + 진행률 | 핵심 루프 완성 | 1일 |
| **M3** | 카테고리 + 필터 | 분류·필터링 동작 | 1일 |
| **M4** | 인라인 수정 + Undo + 백업 | 기능 완성 | 1일 |
| **M5** | 스타일링 + 접근성 + 엣지 케이스 | 배포 가능 상태 | 1~2일 |

> M1~M2만 끝나도 **실사용이 가능**하다. 실제로 며칠 써보고 M3 이후를 조정하는 걸 권한다.

---

## 12. 향후 확장 아이디어 (v2+)

- 마감일 및 오늘/이번 주 뷰
- 드래그 앤 드롭 순서 변경
- 다크 모드
- 키보드 단축키 (`n` 새 항목, `/` 검색 등)
- 완료 통계 (주간 완료 추이 그래프)
- 카테고리 사용자 정의 추가
- IndexedDB[^8] 전환 (데이터 대량화 시)
- PWA[^9] 전환 → 오프라인 설치형 앱

---

## 13. 미결정 사항 (Open Questions)

| # | 질문 | 비고 |
|---|---|---|
| 1 | 완료 항목을 **자정에 자동 정리**할 것인가, 계속 누적할 것인가? | 누적 시 통계 확장에 유리, 정리 시 화면이 깔끔 |
| 2 | 카테고리 3개 고정 vs 사용자 추가 허용? | v1은 고정 권장 |
| 3 | 삭제 확인을 `confirm()` vs Undo 토스트 중 무엇으로? | 본 문서는 Undo 권장 |

---

## 📌 부록. 용어 각주 정리

| 번호 | 용어 | 설명 |
|---|---|---|
| [^1] | **CRUD** | Create(생성)·Read(조회)·Update(수정)·Delete(삭제). 데이터를 다루는 4대 기본 동작을 묶어 부르는 말. |
| [^2] | **localStorage** | 브라우저가 제공하는 저장 공간. 문자열 형태의 키-값 쌍을 저장하며, 브라우저를 껐다 켜도 유지된다. 도메인당 보통 5~10MB. |
| [^3] | **영속성(Persistence)** | 프로그램이 종료되어도 데이터가 사라지지 않고 남아 있는 성질. |
| [^4] | **수용 기준(Acceptance Criteria)** | "이 조건들을 모두 만족해야 이 기능이 완성된 것으로 인정한다"는 판정 기준 목록. 개발 완료 여부를 주관이 아닌 체크리스트로 판단하게 해준다. |
| [^5] | **낙관적 삭제(Optimistic Delete)** | 사용자에게 먼저 물어보지 않고 일단 삭제한 뒤, 되돌릴 기회(Undo)를 주는 방식. 확인 팝업보다 조작 흐름이 빠르다. |
| [^6] | **이벤트 위임(Event Delegation)** | 자식 요소마다 이벤트를 다는 대신 부모 요소에 하나만 걸고, 실제 클릭된 대상을 판별해 처리하는 기법. 목록을 다시 그려도 이벤트를 재등록할 필요가 없다. |
| [^7] | **XSS (Cross-Site Scripting)** | 사용자 입력에 악성 스크립트를 심어 실행시키는 공격. 입력값을 `innerHTML`로 넣으면 발생 가능하며, `textContent`를 쓰면 태그가 문자 그대로 표시되어 차단된다. |
| [^8] | **IndexedDB** | 브라우저 내장 데이터베이스. `localStorage`보다 용량이 크고 구조화된 데이터·검색에 유리하지만 사용법이 복잡하다. |
| [^9] | **PWA (Progressive Web App)** | 웹앱을 네이티브 앱처럼 설치하고 오프라인에서도 쓸 수 있게 만드는 기술 묶음. |
