# Daily Focus 사용법

개인용 참고 문서입니다. 앱 자체 파일(`index.html`/`style.css`/`app.js`)과는 무관하고, 나중에 사용법이 헷갈릴 때 보려고 남겨둔 메모입니다.

## 접속 방법

| 방법 | 주소 | 데이터 저장 |
|---|---|---|
| 배포 주소 (권장) | https://rewire7.github.io/daily-focus/ | 파일 연결 시 로컬 OneDrive 파일 |
| 로컬 파일 직접 열기 | `web_version\index.html` 더블클릭 | 파일 연결 시 로컬 OneDrive 파일 |
| 모바일용(구버전) | 프로젝트 루트 `index.html` | localStorage만 (동기화 안 됨) |

평소에는 **배포 주소**로 들어가서 쓰면 된다. 로컬 파일 열기는 인터넷이 안 될 때만.

## 기본 사용법 (`web_version`)

- **할 일 추가**: 입력창에 쓰고 카테고리 선택 후 `추가` (제목에 "회의", "운동" 같은 키워드가 있으면 카테고리 자동 추천됨)
- **완료 체크**: 왼쪽 체크박스 클릭
- **카테고리 변경**: 항목 오른쪽 색깔 배지(업무/개인/공부) 클릭 → 순서대로 바뀜
- **수정**: ✏️ 아이콘 클릭 또는 제목 더블클릭 → Enter 저장, Esc 취소
- **삭제**: 🗑 아이콘 클릭 (3초 안에 토스트에서 "실행 취소" 가능)
- **필터**: 상단 카테고리별/완료·미완료 버튼으로 목록 좁혀보기
- **다크 모드**: 헤더 🌙/☀️ 버튼
- **완료된 항목 한번에 삭제**: 목록 맨 아래 버튼

## 백업 (내보내기 / 가져오기)

- **내보내기**: 헤더 `내보내기` 버튼 → `todos-YYYYMMDD.json` 파일로 다운로드됨. 가끔 한 번씩 눌러서 별도 백업해두면 안전.
- **가져오기**: 헤더 `가져오기` 버튼 → 백업 파일 선택 → 확인하면 현재 데이터를 통째로 교체.

## OneDrive로 회사·집 동기화하기 ⭐

가장 헷갈리기 쉬운 부분. 원리: **OneDrive가 파일 하나를 양쪽 컴퓨터에 동기화**해주고, 앱은 그 파일을 읽고 쓸 뿐이다.

### 최초 연결 (한 컴퓨터에서)
1. 배포 주소 접속
2. 헤더 `🔗 파일 연결` 클릭
3. OneDrive 폴더 안(예: `web_version` 폴더)에 `daily-focus-data.json` 이름으로 저장/선택
4. 헤더 아래 파란 배너에 `🔗 연결됨: daily-focus-data.json` 표시되면 완료

### 다른 컴퓨터(예: 회사)에서 이어서 쓰기
1. 그 컴퓨터의 OneDrive가 해당 파일을 다 받았는지 확인 (OneDrive 아이콘이 "동기화 중"이 아니라 "최신 상태"인지)
2. 같은 배포 주소 접속
3. `🔗 파일 연결` 클릭 (이 컴퓨터에서는 처음이라 매번 한 번씩 눌러야 함 — 자동으로 안 넘어감)
4. **똑같은 파일**(`daily-focus-data.json`)을 선택 → 기존 데이터를 불러올지 물으면 "확인"

### 매번 확인할 것
- 데이터 바꾼 직후 → **자리 뜨기 전에 OneDrive 업로드 완료 확인**
- 다른 컴퓨터에서 열기 전 → **그 컴퓨터의 OneDrive 다운로드 완료 확인**
- 업로드/다운로드가 덜 된 상태에서 열면 예전 데이터가 보일 수 있음 (앱 버그 아님, 동기화 대기 문제)

### 헤더 아래 배너가 이렇게 뜨면
- **파란 배너 "연결됨: 파일명"** → 정상, 그 파일에 저장 중
- **"재연결이 필요합니다"** → 브라우저가 권한을 다시 확인하려는 것(재부팅/장시간 후 흔함). `재연결` 버튼 클릭하면 됨
- **배너 없음** → 아직 연결 안 됨(또는 연결 해제함), localStorage에만 저장 중 → 그 기기에서만 보임

### 연결 해제하고 싶을 때
파란 배너의 `연결 해제` 클릭 → 그 순간 데이터는 그대로 유지되고 localStorage 모드로 돌아감(파일 동기화만 끊김).

### 참고
- 이 기능은 **Chrome/Edge에서만** 동작함(파일 연결 버튼이 아예 안 보이면 그 브라우저에선 지원 안 되는 것).
- 두 컴퓨터에서 **동시에** 수정하면 나중에 저장한 쪽이 이김(충돌 자동 병합 없음) — 가급적 한쪽만 쓰고 있을 때 수정할 것.
- 실제 데이터 파일(`daily-focus-data.json`)은 git에 올라가지 않도록 `.gitignore`에 등록되어 있음 — 안심하고 로컬/OneDrive 어디에 둬도 됨.
